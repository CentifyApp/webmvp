import 'package:flutter/material.dart';

void main() => runApp(const twoPlayerBet());

class twoPlayerBet extends StatelessWidget {
  const twoPlayerBet({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Centify"),
      ),
      body: Center(child: ),
    );
  }
}
